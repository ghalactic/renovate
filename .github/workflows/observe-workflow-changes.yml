name: Observe workflow changes

on:
  push:
    paths: [.github/workflows/renovate.yml]
    branches: [main]

jobs:
  report:
    uses: ghalactic/repos/.github/workflows/shared-report-workflow-change-metrics.yml@main
    secrets: inherit
